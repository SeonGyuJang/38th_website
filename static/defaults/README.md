# 기본 파일 폴더

이 폴더는 서버 시작 시 자동으로 `uploads/` 폴더로 복사되는 기본 파일들을 저장합니다.

## 파일 구조

- `logo.png` - 웹사이트 로고 (필수)
- `banners/` - 기본 배너 이미지
- `regulations/` - 기본 회칙 파일

## 사용 방법

1. 기본으로 표시할 파일을 이 폴더에 저장
2. 서버를 시작하면 자동으로 `static/uploads/`로 복사됨
3. Git에 포함되므로 배포 시에도 유지됨
